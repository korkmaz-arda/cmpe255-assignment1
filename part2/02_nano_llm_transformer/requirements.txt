# PyTorch CUDA wheel (RTX 50-series / Blackwell sm_120 needs CUDA >= 12.8). Install it first:
#   pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cu130
# (CPU-only machines: pip install torch==2.14.0)
torch==2.14.0
numpy>=1.26
fastapi>=0.110
uvicorn>=0.29
huggingface_hub>=0.23
pyarrow>=15
pytest>=8
httpx>=0.27
