[pytest]
testpaths = tests
addopts = -m "not e2e" -p no:cacheprovider
markers =
    e2e: browser end-to-end tests (Playwright + Chromium); run with `pytest -m e2e`
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated
